---
import LayoutContainer from "../components/LayoutContainer.astro";

type Props = {
  class?: string;
  fluid?: boolean;
};

const { class: classes, fluid = false } = Astro.props;
---

<LayoutContainer class={`${classes} py-12 lg:py-16 xl:py-20`} fluid={fluid}>
  <slot />
</LayoutContainer>
