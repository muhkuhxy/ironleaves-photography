---
import LayoutContainer from "./LayoutContainer.astro"

type Props = {
  class?: string
}

const { class: classes } = Astro.props
---

<figcaption
  class:list={[
    "caption absolute bottom-0 h-[max-content] w-max text-white drop-shadow",
    classes,
  ]}
>
  <LayoutContainer class="mb-6 sm:mb-12">
    <slot />
  </LayoutContainer>
</figcaption>
