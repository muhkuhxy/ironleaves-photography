---
type Props = {
  class?: string;
  animation?: { name: "slide-up"; delay?: number };
};

const { class: classes, animation } = Astro.props;
---

<figure
  class:list={["relative overflow-hidden", classes]}
  data-animation={animation?.name}
  data-delay={animation?.delay}
>
  <slot />
</figure>
